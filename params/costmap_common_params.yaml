obstacle_range: 2.5
raytrace_range: 3.0
# footprint: [[x0, y0], [x1, y1], ... [xn, yn]]
robot_radius: 0.28
resolution: 0.3
# inflation_radius: 1.5

observation_sources: front_lidar # point_cloud_sensor

front_lidar: {sensor_frame: lidar, data_type: LaserScan, topic: robot/laser_scan, marking: true, clearing: true}

# point_cloud_sensor: {sensor_frame: frame_name, data_type: PointCloud, topic: topic_name, marking: true, clearing: true}
